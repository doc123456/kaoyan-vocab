{
  "name": "kaoyan-vocab-cloud",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --omit=dev && cd ../frontend && npm install --package-lock=false && npm run build",
    "start": "node backend/src/app.js"
  }
}
